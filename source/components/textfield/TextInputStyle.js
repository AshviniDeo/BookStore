import {StyleSheet} from 'react-native';
import {COLOR, MARGIN} from '../../res/GlobalStyle';

export const styles = StyleSheet.create({
  textBox: {
    margin: MARGIN.margin,
    backgroundColor: 'rgba(0,0,0,0)',
  },
  errorText: {
    color: COLOR.googleBtn,
  },
});
